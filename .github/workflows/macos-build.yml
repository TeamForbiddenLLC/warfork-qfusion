name: MacOS Build

on:
  workflow_call:
    inputs:
      config:
        description: 'Build configuration: release or debug'
        type: string
        default: 'release'
      upload_sentry:
        description: 'Upload debug files to Sentry'
        type: boolean
        default: false
        required: false
    secrets:
      SDK_PASSWORD:
        required: true
      SENTRY_AUTH_TOKEN:
        required: false

jobs:
  osx:
    name: "OSX-x86_64-${{ inputs.config }}"
    runs-on: macos-14
    env:
      CONFIG_NAME: "OSX-x86_64-${{ inputs.config }}"
      BUILD_FOLDER: "${{ inputs.config == 'release' && 'Release' || 'Debug' }}"
    steps:
      - name: Replace problem-causing python installation - https://github.com/TeamForbiddenLLC/warfork-qfusion/pull/325 - Fixed in macos-14
        shell: bash
        run: |
          brew uninstall --force azure-cli aws-sam-cli
          brew install python@3 || brew link --overwrite python@3
          brew install cmake git zip unzip libidn2 curl

      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: 15.0.1

      - name: Download steamworks sdk
        run: |
          curl -L https://github.com/Warfork/sdk_internal/raw/refs/heads/main/sdk.zip --output third-party/steamworks/sdk.zip
          unzip -P "$SDK_PASSWORD" third-party/steamworks/sdk.zip -d third-party/steamworks
        env:
          SDK_PASSWORD: ${{ secrets.SDK_PASSWORD }}

      - name: Build
        working-directory: ./source
        run: |
          cmake -B ./build --preset workflow-macos-${{ inputs.config }} -G Xcode
          cd build
          xcodebuild -project qfusion.xcodeproj/ -jobs 4 -configuration ${{ env.BUILD_FOLDER }} -target ALL_BUILD CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO

      - name: Package Assets
        working-directory: ./source
        run: cmake --build ./build --target deploy --config ${{ env.BUILD_FOLDER }}
          
      - name: Package warfork
        working-directory: ./source/build/warfork-qfusion
        run: tar --exclude='test' -czvf ../${{ env.CONFIG_NAME }}.tar.gz ${{ env.BUILD_FOLDER }}/*.app

      - name: Upload warfork artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.CONFIG_NAME }}
          path: source/build/${{ env.CONFIG_NAME }}.tar.gz

      - name: Sentry Upload
        if: inputs.upload_sentry == true
        working-directory: ./source/build/warfork-qfusion
        run: |
          curl -sL https://sentry.io/get-cli/ | bash
          sentry-cli --version
          sentry-cli debug-files upload ./
        env:
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_ORG: team-forbidden-llc
          SENTRY_PROJECT: warfork
